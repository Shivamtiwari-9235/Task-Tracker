# Task Tracker MERN - TODO

## Plan Summary
- Generate MERN Task Tracker from scratch in empty `Backend/` and `Frontend/`.
- Implement mandatory CRUD + pagination + validation + REST APIs + MongoDB integration + responsive UI + dynamic updates.
- Implement bonus: filtering/sorting/search, stats dashboard, export JSON/CSV, skeleton loading, toast notifications, dark mode.
- Add README and .env.example for backend and frontend.

## Steps
1. Create backend project files (`backend/`): package.json, server.js, db config, model, routes, controller, error middleware, .env.example.
2. Implement backend APIs with express-validator + consistent error handling + pagination/filters/sort/search + indexes.
3. Create frontend project files (`frontend/`): package.json, Vite/Tailwind config, src entry files.
4. Implement frontend components/pages/services/context: CRUD UI, validation, loading states, toasts, pagination, filters/sort/search, stats, export, dark mode.
5. Add shared UX helpers: error boundary, notification/toast system, reusable components.
6. Create root README.md with full setup and deployment instructions.
7. Add environment variable examples for backend and frontend.
8. Run and verify locally: start backend (5000) and frontend (5173); test CRUD, filters, pagination, export.

## Progress
- [x] Create backend structure + core server/model/routes/controller/error middleware.
- [ ] Create remaining backend files (if any) + frontend project + UI/components.


